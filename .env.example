# Copie este arquivo para .env e preencha com as credenciais do seu banco.
#   cp .env.example .env
# Depois restrinja a permissão:
#   chmod 600 .env
DB_HOST=localhost
DB_NOME=crud_python
DB_USUARIO=seu_usuario_mysql
DB_SENHA=sua_senha_mysql
